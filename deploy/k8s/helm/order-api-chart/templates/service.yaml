apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-api-chart.fullname" . }}
  labels:
    {{- include "order-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "order-api-chart.selectorLabels" . | nindent 4 }}
