image:
  repository: ghcr.io/onurkanbakirci/tesodev.case/customer-api
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1.0.0"

imagePullSecrets: ""
nameOverride: ""
fullnameOverride: ""